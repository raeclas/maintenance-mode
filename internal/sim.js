// Deterministic EV sim + baseline drift tracker (--compare).
// Law: exists from the FIRST gameplay commit. Every faucet modeled or gated.
// Pattern reference: ../FightingInc/internal/sim.js + baseline.json.
console.log('sim: no game to simulate yet');
